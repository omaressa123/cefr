# CEFR Practice Partner — pinned. The previous unpinned file was a regression:
# an unpinned `gradio` will pull the next major version on the next Space
# rebuild and break Chatbot(type="messages"), and unpinned edge-tts is how you
# find out about a Microsoft handshake change in production.

fastapi
uvicorn[standard]
pydantic==2.12.3
httpx>=0.27,<1.0

gradio==6.26.0
groq==0.33.0
supabase>=2.15.0,<3.0.0
google-genai>=1.0.0

# Deliberately allowed to float UPWARD only. edge-tts talks to an undocumented
# Microsoft endpoint whose auth handshake changes every few months; when TTS
# dies in production, bump this first. Replacing it with Piper is the single
# highest-value offline swap available (see docs/OFFLINE_MIGRATION.md).
edge-tts>=7.0.2,<8.0.0

# --- optional: local forced alignment (ALIGNMENT_ENABLED=true) ---
#   pip install "torch>=2.1" "torchaudio>=2.1"
# --- optional: Azure unscripted pronunciation (AZURE_SPEECH_KEY set) ---
#   pip install "azure-cognitiveservices-speech>=1.40"
# --- optional: offline TTS (recommended, see docs/OFFLINE_MIGRATION.md) ---
#   pip install piper-tts
